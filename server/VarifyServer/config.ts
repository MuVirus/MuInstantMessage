import config from "./config.json";

export const email_user = config.email.user;
export const email_pass = config.email.pass;

export const mysql_host = config.mysql.host;
export const mysql_post = config.mysql.port;
export const mysql_pass = config.mysql.passwd;

export const redis_host = config.redis.host;
export const redis_port = config.redis.port;
export const redis_pass = config.redis.passwd;
